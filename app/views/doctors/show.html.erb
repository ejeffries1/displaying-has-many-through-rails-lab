
<% @doctor.appointments.each do |doctor| %>
    <%= doctor.appointment_datetime.strftime("%B %d, %Y at %k:%M") %><br>

<% end %>

<% @doctor.patients.each do |patient| %>
    
    <%= link_to patient.name, patient_path(patient)%>
<% end %>